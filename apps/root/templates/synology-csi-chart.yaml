apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: synology-csi-chart
  namespace: argocd
spec:
  destination:
    namespace: synology-csi-chart
    server: https://kubernetes.default.svc
  project: default
  source:
    path: apps/synology-csi-chart
    repoURL: https://github.com/halkeye/home-k8s
    targetRevision: HEAD
    helm:
      valueFiles:
        - values.yaml
        - secrets://values-secrets.yaml
  syncPolicy:
    automated:
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
