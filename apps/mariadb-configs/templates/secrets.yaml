apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mariadb-operator.fullname" . }}-secrets
  labels:
    {{- include "mariadb-operator.labels" . | nindent 4 }}
type: Opaque
stringData:
  root-password: {{ .Values.rootPassword | quote }}
  password: {{ .Values.password | quote }}
