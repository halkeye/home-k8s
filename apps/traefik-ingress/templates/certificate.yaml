apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: g4v-wildcard-certificate
spec:
  commonName: {{ .Values.cert.commonName }}
  dnsNames: {{ .Values.cert.cnsNames | toJson }}
  issuerRef:
    kind: ClusterIssuer
    name: acme-letsencrypt-prod
  secretName: wildcard-g4v-tls
