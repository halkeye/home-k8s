apiVersion: "acid.zalan.do/v1"
kind: postgresql
metadata:
  name: postgres
spec:
  teamId: "acid"
  volume:
    size: 10Gi
    storageClass: synology-csi-retain
  numberOfInstances: 2
  postgresql:
    version: "15"
  users:
    halkeye:  # database owner
    - superuser
    - createdb
