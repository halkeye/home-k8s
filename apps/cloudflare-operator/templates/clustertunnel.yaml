{{- range $tunnel := .Values.tunnels }}
---
apiVersion: networking.cfargotunnel.com/v1alpha1
kind: ClusterTunnel
metadata:
  name: {{ $tunnel.name }}
spec: {{ toYaml $tunnel | nindent 2 }}
{{- end }}
