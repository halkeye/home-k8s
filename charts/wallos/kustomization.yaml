---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmGlobals:
  chartHome: ./charts/

generatorOptions:
  disableNameSuffixHash: true

helmCharts:
  - name: app-template
    version: 4.6.0
    repo: https://bjw-s-labs.github.io/helm-charts
    valuesFile: values.yaml
    releaseName: wallos

configMapGenerator:
  - name: litestream-configmap
    literals:
      - LITESTREAM_DB_PATH="/data/wallos.db"
      - |
        litestream.yml=
          dbs:
            - path: /data/wallos.db
              replicas:
                - url: s3://halkeye-litestream.tor1.digitaloceanspaces.com/wallos

components:
  - ../../components/litestream
