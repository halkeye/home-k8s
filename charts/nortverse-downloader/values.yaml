controllers:
  main:
    type: cronjob
    revisionHistoryLimit: 1
    annotations:
      reloader.stakater.com/auto: 'true'
    cronjob:
      schedule: '@daily'
      successfulJobsHistory: 1
      failedJobsHistory: 3
    containers:
      main:
        image:
          repository: ghcr.io/halkeye/nortverse-downloader
          tag: 1.4.0
        args:
          - download
          - "--output"
          - /webcomics/Nortverse/
          - "--start-url"
          - "https://nortverse.com/comic/healthy-compromise/"
    pod:
      securityContext:
        runAsUser: 1026
        runAsGroup: 101
        runAsNonRoot: true
        fsGroup: 101
        fsGroupChangePolicy: OnRootMismatch
persistence:
  webcomics:
    type: nfs
    server: 172.16.10.10
    path: /volume1/Comics/Webcomics
    globalMounts:
      - path: /webcomics
