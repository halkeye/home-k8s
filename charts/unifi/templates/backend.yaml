apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: unifi-https
spec:
  endpoints:
    - fqdn:
        hostname: unifi.unifi.svc.cluster.local
        port: 8443
  tls:
    insecureSkipVerify: true
