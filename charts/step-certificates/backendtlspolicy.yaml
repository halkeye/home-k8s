---
apiVersion: gateway.networking.k8s.io/v1alpha3
kind: BackendTLSPolicy
metadata:
  name: step-certificates
spec:
  targetRefs:
    - group: ""
      kind: Service
      name: step-certificates
  options:
    envoy.io/skip-server-cert-verification: "true"
  validation:
    hostname: localhost
