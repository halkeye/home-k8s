controllers:
  main:
    annotations:
      reloader.stakater.com/auto: "true"
    containers:
      main:
        image:
          repository: ghcr.io/halkeye/stashapp-prometheus-exporter
          tag: sha-2581b55
        envFrom:
          - secretRef:
              name: "stashapp-exporter"
        probes:
          liveness:
            enabled: true
          readiness:
            enabled: true
          startup:
            enabled: true
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop: ["ALL"]
        resources:
          requests:
            cpu: 15m
            memory: 60M
    pod:
      securityContext:
        runAsUser: 65535
        runAsGroup: 65535
        runAsNonRoot: true
        fsGroup: 65535
        fsGroupChangePolicy: OnRootMismatch
service:
  main:
    controller: main
    ports:
      http:
        port: 9100
serviceMonitor:
  app:
    serviceName: stashapp-exporter
    endpoints:
      - port: http
        scheme: http
        path: /metrics
