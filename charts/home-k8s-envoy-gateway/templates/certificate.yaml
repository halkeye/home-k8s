---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: g4v-home-wildcard-certificate
spec:
  dnsNames:
    - '*.g4v.dev'
    - '*.h.g4v.dev'
  commonName: "*.g4v.dev"
  issuerRef:
    name: acme-letsencrypt-prod
    kind: ClusterIssuer
  secretName: wildcard-home-g4v-tls
