# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/library/common/values.schema.json
controllers:
  main:
    replicas: 1
    annotations:
      reloader.stakater.com/auto: "true"
    containers:
      main:
        image:
          repository: docker.io/excalidraw/excalidraw
          tag: latest@sha256:0295d7a1614afac68e6c602da6d7168c1505c9926eefcee0fcce9c5eadd42052
        env:
          TZ: America/Vancouver
service:
  main:
    controller: main
    ports:
      http:
        port: 80
route:
  main:
    enabled: true
    parentRefs:
      - name: http-gateway
        namespace: envoy-gateway
        sectionName: https
    hostnames:
      - "draw.g4v.dev"
    rules:
      - matches:
        - path:
            type: PathPrefix
            value: /
        backendRefs:
          - identifier: main
            port: http
