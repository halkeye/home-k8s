apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hubble-ui
spec:
  parentRefs:
    - name: tls-home-gateway 
      namespace: cilium
      sectionName: https
  hostnames:
    - hubble-ui.h.g4v.dev
  rules:
    - backendRefs:
        - name: hubble-ui
          port: 80
