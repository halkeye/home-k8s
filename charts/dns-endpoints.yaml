apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: nas
  annotations:
    external-dns: public
spec:
  endpoints:
  - dnsName: "*.nas.g4v.dev"
    recordTTL: 180
    recordType: A
    targets:
    - 172.16.10.10
