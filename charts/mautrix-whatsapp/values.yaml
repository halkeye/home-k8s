controllers:
  main:
    annotations:
      reloader.stakater.com/auto: "true"
    replicas: 1
    type: statefulset
    containers:
      main:
        image:
          repository: dock.mau.dev/mautrix/whatsapp
          tag: v0.2605.0
        env:
          TZ: America/Vancouver
          BRIDGE_DATABASE__URI:
            valueFrom:
              secretKeyRef:
                name: "mautrix-whatsapp-db-secret-mautrix-whatsapp"
                key: POSTGRES_URL
        envFrom:
          - secretRef:
              name: "mautrix-whatsapp-secret"
        command:
          - /usr/bin/mautrix-whatsapp
          - "-n"
          - "-c"
          - /configmap/config.yaml
        probes:
          liveness:
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /_matrix/mau/live
                port: 29318
              initialDelaySeconds: 0
              periodSeconds: 10
              timeoutSeconds: 1
              failureThreshold: 3
          readiness:
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /_matrix/mau/ready
                port: 29318
              initialDelaySeconds: 0
              periodSeconds: 10
              timeoutSeconds: 1
              failureThreshold: 3
          startup:
            enabled: false
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop: ["ALL"]
        resources:
          limits:
            memory: 100Mi
          requests:
            cpu: 10m
            memory: 100Mi
    pod:
      securityContext:
        runAsUser: 1337
        runAsGroup: 1337
        runAsNonRoot: true
        fsGroup: 1337
        fsGroupChangePolicy: OnRootMismatch

service:
  main:
    controller: main
    publishNotReadyAddresses: true
    ports:
      http:
        port: 29318

persistence:
  tmp:
    enabled: true
    type: emptyDir
  data:
    enabled: true
    type: emptyDir
  configmap:
    enabled: true
    type: secret
    name: "mautrix-whatsapp-secret"

route:
  main:
    enabled: true
    parentRefs:
      - name: http-gateway
        namespace: envoy-gateway
        sectionName: https
    hostnames:
      - "mautrix-bridges.g4v.dev"
    rules:
      - matches:
        - path:
            type: PathPrefix
            value: /whatsapp
        filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /
        backendRefs:
          - identifier: main
            port: http
