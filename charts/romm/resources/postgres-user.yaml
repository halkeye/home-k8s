apiVersion: db.movetokube.com/v1alpha1
kind: PostgresUser
metadata:
  name: romm
spec:
  role: romm
  database: romm
  secretName: romm-db
  privileges: OWNER
  secretTemplate:
    HOST_NO_PORT: "{{ .HostNoPort }}"
    PORT: "{{ .Port }}"
    
