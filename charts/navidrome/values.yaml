---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/library/common/values.schema.json
defaultPodOptions:
  enableServiceLinks: false
  securityContext:
    runAsUser: 1026
    runAsGroup: 100
    runAsNonRoot: true
    fsGroup: 100
    fsGroupChangePolicy: "OnRootMismatch"

controllers:
  navidrome:
    annotations:
      reloader.stakater.com/auto: "true"
      litestream: enabled
    containers:
      oauth2proxy:
        image: 
          repository: quay.io/oauth2-proxy/oauth2-proxy
          tag: v7.14.3
        env:
          OAUTH2_PROXY_CODE_CHALLENGE_METHOD: S256
          OAUTH2_PROXY_COOKIE_DOMAINS: "navidrome.g4v.dev"
          OAUTH2_PROXY_EMAIL_DOMAINS: "*"
          OAUTH2_PROXY_HTTP_ADDRESS: "http://[::]:4180"
          OAUTH2_PROXY_LOGIN_URL: https://auth.g4v.dev
          OAUTH2_PROXY_OIDC_ISSUER_URL: https://auth.g4v.dev/application/o/navidrome/
          OAUTH2_PROXY_PROVIDER: oidc
          OAUTH2_PROXY_REVERSE_PROXY: "true"
          OAUTH2_PROXY_SCOPE: openid profile email
          OAUTH2_PROXY_SET_XAUTHREQUEST: "true"
          OAUTH2_PROXY_SILENCE_PING_LOGGING: "true"
          OAUTH2_PROXY_SKIP_PROVIDER_BUTTON: "true"
          OAUTH2_PROXY_UPSTREAM: "http://localhost:8080"
          OAUTH2_PROXY_WHITELIST_DOMAINS: "navidrome.g4v.dev"
          OAUTH2_PROXY_XAUTHREQUEST: "true"
        envFrom:
          - secretRef:
              name: navidrome-oauth2-proxy
        probes:
          liveness:
            enabled: true
          readiness:
            enabled: true
        resources:
          limits:
            memory: 110Mi
          requests:
            cpu: 30m
            memory: 110Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
          readOnlyRootFilesystem: true
      navidrome:
        image:
          repository: ghcr.io/navidrome/navidrome
          tag: 0.60.3
        env:
          TZ: America/Vancouver
          ND_AUTHREQUESTLIMIT: 0
          ND_COVERJPEGQUALITY: "100"
          ND_DATAFOLDER: /config
          ND_DEFAULTTHEME: Dark
          ND_ENABLEGRAVATAR: "true"
          ND_ENABLESHARING: "true"
          ND_ENABLETRANSCODINGCONFIG: "true"
          ND_EXTAUTH_TRUSTEDSOURCES: "0.0.0.0/0"
          ND_EXTAUTH_USERHEADER: X-Auth-Request-User
          ND_IMAGECACHESIZE: "1GB"
          ND_LASTFM_ENABLED: "true"
          ND_PORT: 8080
          ND_PROMETHEUS_ENABLED: "true"
          ND_SCANSCHEDULE: "@every 1h"
          ND_SEARCHFULLSTRING: "true"
          ND_SESSIONTIMEOUT: 24h
          ND_SUBSONICARTISTPARTICIPATIONS: "true"
        envFrom:
          - secretRef:
              name: navidrome-secret
        probes:
          liveness:
            enabled: true
          readiness:
            enabled: true
        resources:
          requests:
            cpu: 10m
            memory: 250Mi
          limits:
            memory: 1Gi

service:
  main:
    controller: navidrome
    ports:
      http:
        port: 8080
      oauth:
        port: 4180

route:
  main:
    enabled: true
    parentRefs:
      - name: http-gateway
        namespace: envoy-gateway
        sectionName: https
    hostnames:
      - "navidrome.g4v.dev"
    rules:
      - matches:
        - path:
            type: PathPrefix
            value: /
        backendRefs:
          - identifier: main
            port: oauth
      - matches:
        - path:
            type: PathPrefix
            value: /share/
        backendRefs:
          - identifier: main
            port: http
      - matches:
        - path:
            type: PathPrefix
            value: /rest/
        backendRefs:
          - identifier: main
            port: http

persistence:
  data:
    enabled: true
    type: persistentVolumeClaim
    accessMode: ReadWriteOnce
    size: 10Gi
    globalMounts:
      - path: /config
  music:
    type: nfs
    server: 172.16.10.10
    path: /volume1/Audio/Music
    globalMounts:
      - path: /music
  configmap:
    type: configMap
    name: navidrome-configmap
    globalMounts:
      - path: /data/navidrome.toml
        subPath: navidrome.toml
        readOnly: true
  litestream-configmap:
    type: configMap
    name: litestream-configmap
    globalMounts: []
